#My simple boilerplate Gulp + Rigger + Sass + Compass + LiveReload

##Usage:

1. Download this to your working folder

2. run in terminal `npm install`

3. `bower i`

4. `gulp`

5. Go to [http://localhost:9000/build/](http://localhost:9000/build/)